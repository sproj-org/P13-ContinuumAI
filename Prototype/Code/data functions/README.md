data_functions.py is the file with all the finalised functions.

plotly has an in-built functionality that allowes you to convert a graph into json format, which can then easily be rendered on the frontend via the plotly js lib.
useful git repo for frontend visualization: https://github.com/nikita715/plotly-json-visualizer

vis tool: https://nikita715.github.io/plotly-json-visualizer/

plotly_outputs folder: has json outputs for each func.

demo_sales.csv: same as proto1